services:
  - type: web
    name: aquamizer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --worker-class sync --workers 1 --timeout 0
    envVars:
      - key: TZ
        value: America/Los_Angeles